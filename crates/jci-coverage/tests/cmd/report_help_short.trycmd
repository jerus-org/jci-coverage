```
$ jci-coverage report -h
Generate a coverage report with cargo-llvm-cov

Usage: jci-coverage report [OPTIONS]

Options:
      --package <NAME>
          Limit coverage to one workspace package
  -v, --verbose...
          Increase logging verbosity
  -q, --quiet...
          Decrease logging verbosity
      --runner <RUNNER>
          Test runner to drive coverage with [default: test] [possible
          values: test, nextest]
      --nextest-profile <NAME>
          Nextest profile to use
      --no-all-features
          Disable `--all-features` (on by default)
      --dry-run
          Preview the cargo-llvm-cov invocation without running it
  -h, --help
          Print help (see more with '--help')

```
